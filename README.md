# GIHUB-README-STUDIO

A visual, drag-and-drop builder for creating GitHub Profile READMEs.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

Then open `http://localhost:3000` in your browser.
